# Massoh repo ignores
.massoh-backups/
*.tmp
.DS_Store
.agent_tasks/cron/cron.log

# LibreOffice lock files
.~lock.*#

# Decide-or-defer runtime artifacts (v0.28.0) — append-only, kept locally, not committed
NOTIFICATIONS.md
DECISIONS.md
.agent_tasks/cron/decisions.queue
