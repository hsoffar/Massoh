# STANDARDS — {{PROJECT}} (how we build here)

> Generated by `massoh discover` on {{DATE}} from a heuristic scan. **Review + fill the `(…)`
> placeholders** — the scan guesses; you confirm. This is the "how", not the "what" (specs live in
> task packets). `massoh-implementer` and `massoh-reviewer-qa` read this before touching code.

## Stack (detected)
- Languages / build markers: {{LANGS}}
- Test command: {{TESTCMD}}
- Commit convention: {{COMMITCONV}}

## Top-level layout (detected)
{{LAYOUT}}

## Conventions (confirm / fill in)
- Naming: ( … e.g. files, functions, branches )
- Error handling: ( … the project's pattern )
- Logging / observability: ( … )
- Tests: what counts as a real test here; where they live; how to run the full suite.
- Dependencies: ( … add-a-dep policy, lockfile rules )

## Architectural patterns (the reasoning, not just the rule)
- ( … the patterns refined over time, and *why* — the tribal knowledge that only lives in the code )
- Swap seams / extension points: ( … where new code plugs in safely )

## Do / Don't
- Do: ( … )
- Don't: ( … the mistakes that bit us before )

## Refresh
Re-run `massoh discover --force` after a significant stack change; re-confirm the placeholders.
