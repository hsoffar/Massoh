# METRICS + activation — {{PROJECT}}

Product Scope requires every "Build" to name an event here, or the answer is Defer.

## Funnel / activation events (named)
| Event | Meaning | Stage |
|---|---|---|
| {{ e.g. signup }} | … | acquisition |
| {{ e.g. first_core_action }} | … | activation |
| {{ … }} | … | retention |

## Activation definition
{{ the specific event chain + window that = an activated user }}

## What we do NOT instrument yet
{{ deferred events — named so the design accounts for them even if wiring is later }}
